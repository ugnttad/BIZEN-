{
  "installCommand": "npm ci --prefix BE && npm ci --prefix FE",
  "buildCommand": "npm --prefix FE run build",
  "outputDirectory": "FE/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
